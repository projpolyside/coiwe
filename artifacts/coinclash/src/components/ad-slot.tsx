// Ad slot placeholder — kept as a no-op in the Vite migration.
// Replace with real Google AdSense implementation if needed.
export function AdSlot({
  format,
  slotId,
}: {
  format?: "horizontal" | "rectangle"
  slotId?: string
}) {
  // Silently render nothing — avoids breaking layouts
  return null
}
